``` mermaid
sequenceDiagram
    participant browser as Browser
    participant server as Server

    Note right of browser: The user writes a new note and clicks "Save"

    Note right of browser: JavaScript intercepts the form submission (preventDefault)

    browser->>browser: Create note object {content, date}
    browser->>browser: Update local notes array and redraw page (DOM updated)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (JSON data)
    activate server
    server-->>browser: HTTP 201 Created
    deactivate server

    Note right of browser: The page does not reload. The new note is already visible from the updated local state

```